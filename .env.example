# Figma personal access token used by the Code Connect CLI.
# Required scopes: Code Connect (Write) and File content (Read).
# Copy this file to .env and replace the placeholder. Never commit the real token.
FIGMA_ACCESS_TOKEN=figd_replace_with_your_personal_access_token